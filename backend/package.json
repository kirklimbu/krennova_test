{
  "name": "test",
  "version": "1.0.0",
  "description": "Angualr 8 demo app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "api": "json-server db.json --routes routes.json --no-cors=true",
    "generate": "node generate.js > database.json",
    "server": "json-server --watch database.json"
  },
  "author": "kirk limbu",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "multer": "^1.4.2"
  },
  "devDependencies": {}
}
